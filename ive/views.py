from django.shortcuts import render

# Create your views here.
def show_one(request):
    context = {
        'name': '원영',
        'urls': ''
    }

def show_you(request):
    context = {
        'name': '유진',
        'urls': ''
    }